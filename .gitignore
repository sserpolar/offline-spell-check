dist/
__pycache__/
*.pyc
screenshots/raw/

# ── 内部策略文档:**故意不公开** ────────────────────────────────────
# 这四份里有竞品分析(在位者的评分 / 差评口径 / chrome-stats 风险标记)、
# 上架时机策略、以及「我们故意不承诺什么」那一节。
# 都不是机密,引用的也都是公开事实 —— 但公开发布等于把路线图递给对手,
# 而且那节「有意留白」是能被人拿去引用的。
#
# ⚠️ git 历史是永久的、GitHub 还会缓存和被 fork,所以这个决定必须在
#    **第一次 push 之前**做完。2026-08-17 已决定:留本地。
#
# 它们仍然是活文档,继续在 D:\sidehustle\spell-ext\ 本地维护。
# 换机器时手动带过去(或另开一个 private 仓库专放这类东西)。
TODO.md
STORE_LISTING.md
STORE_LISTING_EDGE.md
SUBMIT_CHECKLIST.md
